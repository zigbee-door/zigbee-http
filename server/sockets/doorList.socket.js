/**
 * describe: doorList页面的socket长连接服务
 * data:     16.12.27
 * author:   zhuxiankang
 * parm:     socket
 */
const event = require('../events')
    , channel = require('../constants/redis.constant');


module.exports = (socket) => {
    // socket.on(channel.doorList_update,  (data) => {
    //
    // });
};